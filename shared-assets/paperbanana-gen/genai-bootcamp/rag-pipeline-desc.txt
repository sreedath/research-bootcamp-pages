A detailed architecture diagram of a Retrieval-Augmented Generation (RAG) pipeline. Show the complete flow: (1) Document Ingestion: raw documents are split into chunks using various chunking strategies, (2) Embedding: chunks are converted to dense vector embeddings using an LLM encoder, (3) Vector Store: embeddings are indexed in a vector database for efficient similarity search, (4) Query Processing: user query is embedded and used for semantic search, (5) Retrieval and Reranking: top-k documents are retrieved and reranked using a cross-encoder, (6) Generation: retrieved context is combined with the query in a prompt template and fed to an LLM for answer generation. Show evaluation metrics (MAP, nDCG) on the side. Use a clean academic style with rectangular boxes, directional arrows, and clear labels. Color scheme: green and cyan accents on white background.
