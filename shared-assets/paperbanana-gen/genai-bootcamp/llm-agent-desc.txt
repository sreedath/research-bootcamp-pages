A detailed architecture diagram of an LLM Agent system built with LangChain. Show the core components: (1) User Input at the top, (2) the LLM (brain of the agent) in the center receiving the input, (3) Tools/Actions available to the agent on one side (web search, code execution, database query, API calls), (4) Memory module on the other side showing both short-term (conversation buffer) and long-term (vector store) memory, (5) Chain orchestration showing how LCEL chains connect components, (6) Guardrails and safety filters wrapping the output. Show the agent reasoning loop: Observe, Think, Act, with arrows forming a cycle. Include prompt engineering elements like system prompts and few-shot examples feeding into the LLM. Use a clean academic style with boxes, arrows, and clear labels. Color scheme: green and cyan accents on white background.
