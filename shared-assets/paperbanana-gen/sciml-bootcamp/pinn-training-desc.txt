A diagram showing the Physics-Informed Neural Network training process. Show a neural network that takes spatiotemporal coordinates (x, t) as input and outputs the solution u(x,t). Show three loss terms being computed: (1) Data loss comparing predictions to observed measurements, (2) Physics loss enforcing the PDE residual to be zero via automatic differentiation, and (3) Boundary/initial condition loss. Show these three terms being combined with weighting factors lambda into a total loss. Show the training loop with gradient descent. Use a clean, publication-quality academic style.
