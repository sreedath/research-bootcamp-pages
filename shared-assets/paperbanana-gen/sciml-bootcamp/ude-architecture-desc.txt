A detailed architecture diagram of a Universal Differential Equation (UDE) showing how known physics terms are kept intact while unknown terms are replaced by neural networks. Show the ODE system du/dt = f_known(u) + NN_theta(u) where f_known represents the known physics and NN_theta is the neural network approximating unknown dynamics. Show the training loop: forward solve the ODE, compute loss against data, backpropagate through the ODE solver using adjoint methods, update neural network weights. Include the symbolic regression step that recovers closed-form equations from the trained neural network. Use clean academic diagram style.
